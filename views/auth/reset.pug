extends ../layouts/main.layout.pug

block content
    div.header__home
        h1 Reset your password
    if errorMessage
        div.error
            p #{errorMessage}
    include ../includes/form.include.pug